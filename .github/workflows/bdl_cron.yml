name: BDL Predict Pipeline

on:
  schedule:
    # 1) 예측 무인 자동 실행: 20:17 KST (1차) & 20:47 KST (2차 백업)
    - cron: '17 11 * * *'
    - cron: '47 11 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  bdl-predict:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Predict Pipeline
        run: |
          python run_bdl.py --mode predict

      - name: Commit and Push Changes to GitHub
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          git add bdl_data.db app.py
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-predict bdl_data.db via GitHub Actions" && git push)
